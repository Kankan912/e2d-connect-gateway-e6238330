export function useRealtimeUpdates({ table, onUpdate, enabled = true }: { 
  table: string; 
  onUpdate: () => void;
  enabled?: boolean;
}) {
  // Placeholder - À implémenter avec Supabase Realtime
  return null;
}

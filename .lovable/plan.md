\n# Plan de correction de l'audit — module par module\n\n## Contexte\n\nL'audit fourni (47 lignes) couvre 11 modules. Une grande partie a déjà été traitée lors des itérations précédentes (refactor email, création compte atomique, ErrorBoundary global, trigger validation reconductions, filtrage `exercices_cotisations_types.actif`, sync matchs E2D → `site_events`, albums galerie liés aux événements, `formatFCFA` qui plancher systématiquement les montants).\n\nCe plan se concentre exclusivement sur les **points résiduels non encore corrigés**, organisés par module avec un statut clair pour chacun.\n\n---\n\n## 1. Module Caisse\n\n| Item audit | État actuel | Action |\n|---|---|---|\n| Décimales FCFA | `formatFCFA` plancher déjà — OK partout sauf logs | Audit complet de tous les `Number(x)` affichés et forçage `Math.floor` côté hook `useCaisse` (champs `solde_global`, `solde_empruntable`, totaux mois) |\n| Solde empruntable double valeur | RPC `get_caisse_synthese` source unique cô